➖ **خونه‌ی `xhttp` — راهنمای کامل**

`xhttp` تنها ترنسپورتیه که **کل جلسه رو با HTTP واقعی حرف می‌زنه**. بقیه (`http` / `https`) فقط اولین تبادل رو شبیه‌سازی می‌کنن و بعدش بایت خام می‌فرستن — و CDN وسط راه اونا رو دور می‌ندازه، چون CDN سوکت فوروارد نمی‌کنه، **درخواست** فوروارد می‌کنه. برای همین فقط `xhttp` از کلادفلیر رد می‌شه.

---

## 🧭 اول از همه: کدوم ایندکس روی کدوم سمت؟

بیشترین اشتباه اینجا اتفاق می‌افته. توی حالت کلادفلیر **جهت اتصال برعکس می‌شه**:

| | حالت عادی | حالت کلادفلیر |
|---|---|---|
| **سرور** (ایران، جایی که یوزرها وصل می‌شن) | لیسن می‌کنه | **دیال می‌کنه** → به edge |
| **کلاینت** (خارج، خروجی اینترنت) | دیال می‌کنه | **لیسن می‌کنه** ← origin کلادفلیره |

نقش‌ها بالاتر از اتصال **هیچ تغییری نمی‌کنن**: سرور همچنان همون‌جاست که یوزر وصل می‌شه (SOCKS5 و port map)، کلاینت همچنان راه خروج به اینترنته. فقط *کی connect می‌زنه* عوض می‌شه.

از همین‌جا این قانون درمیاد:

| ایندکس | سرور | کلاینت | توضیح کوتاه |
|---|:---:|:---:|---|
| `path` | ✅ | ✅ | **باید دقیقاً یکی باشه** |
| `mode` | ✅ | ✅ | فقط سمتی که **دیال می‌کنه** اثر داره |
| `allow_insecure_tls` | ✅ | ➖ | فقط سمت دیال‌کننده + فقط توی حالت CDN |
| `cdn.enabled` | ✅ | ✅ | **باید دو طرف یکی باشه** |
| `cdn.origin_bind` | ➖ | ✅ | فقط کلاینت |
| `cdn.host` | ✅ | ➖ | فقط سرور |
| `cdn.port` | ✅ | ➖ | فقط سرور |
| `cdn.edge_ips` | ✅ | ➖ | فقط سرور |

هر چیزی که ➖ داره، اون سمت **اصلاً خونده نمی‌شه** — گذاشتنش فقط باعث اشتباه می‌شه.

---

## 1️⃣ `path`

پیشوند مسیر URL که هر دو طرف روش توافق دارن. شناسه‌ی جلسه به تهش اضافه می‌شه، یعنی `/api` تبدیل می‌شه به درخواست‌هایی مثل:

```
/api/Gy66C0bzuE0aHOpXvYgeOg?v=r731iuyLg
```

کارش طبیعی‌سازیه: ترافیک تانل شبیه یه API معمولی می‌شه. همچنین اگه روی یه دامنه چند سرویس داری، باهاش جداشون می‌کنی (مثلاً nginx بر اساس path روت کنه).

- **ورودی:** `string`
- **پیش‌فرض:** `/`
- ⚠️ **باید روی هر دو سمت مو به مو یکی باشه**، وگرنه درخواست‌ها به مقصد نمی‌رسن.

---

## 2️⃣ `mode`

شکل ارسال دیتای **آپلود** رو تعیین می‌کنه. سه حالت:

| مقدار | یعنی چی |
|---|---|
| `auto` | اول استریم رو امتحان می‌کنه، اگه مسیر بافرش کرد خودکار می‌ره روی حالت پکتی |
| `stream-up` | اصرار می‌کنه روی یک درخواست POST طولانی (یک استریم پیوسته) |
| `packet-up` | همیشه پکتی: چند POST شماره‌دار پشت سر هم |

**نکته‌ی مهم:** پشت کلادفلیر تقریباً همیشه به `packet-up` می‌رسه — چون کلادفلیر آپلودِ استریمی رو بافر می‌کنه. این محدودیت خودِ CDN ـه، با کانفیگ دور نمی‌شه. پس اگه دیدی رفت روی sequenced uploads، طبیعیه.

- **ورودی:** `string`
- **پیش‌فرض:** `auto`
- فقط روی سمتی اثر داره که **دیال می‌کنه** (توی حالت کلادفلیر = سرور). سمت مقابل هر دو شکل رو قبول می‌کنه.

---

## 3️⃣ `allow_insecure_tls`

بررسی گواهی طرف مقابل رو خاموش می‌کنه (وقتی گواهی self-signed ـه یا مشکل داره).

⚠️ **دو تا نکته که معمولاً اشتباه گفته می‌شه:**

1. **فقط توی حالت CDN اثر داره.** توی حالت مستقیم (بدون کلادفلیر) بررسی گواهی **همیشه خاموشه** — چون اونجا معمولاً گواهی self-signed روی خود سرور نشسته. پس این مقدار اونجا کاری نمی‌کنه.
2. **فقط سمت دیال‌کننده خونده می‌شه.** توی حالت کلادفلیر یعنی **سرور**. گذاشتنش روی کلاینت هیچ اثری نداره.

پشت کلادفلیر گواهی واقعیه، پس مقدار درست `false` ـه.

- **ورودی:** `bool` → `true` / `false`
- **پیش‌فرض:** `false`

---

## 4️⃣ `cdn` — ایندکس داخل `xhttp`

### `cdn.enabled`

سوئیچ اصلی. با `true` کردنش:

- جهت اتصال برعکس می‌شه (جدول بالا)
- سرور از طریق `edge_ips` به کلادفلیر وصل می‌شه
- کلاینت تبدیل می‌شه به origin و منتظر می‌مونه

- **ورودی:** `bool`
- **پیش‌فرض:** `false`
- ⚠️ **حتماً باید روی هر دو سمت یکسان باشه.** اگه یکی `true` و اون یکی `false` باشه، دو طرف در دو جهت مخالف منتظر همدیگه می‌مونن و هیچ‌وقت وصل نمی‌شن.

---

## 5️⃣ `cdn.origin_bind` — 🔵 فقط کلاینت

آدرسی که کلاینت (که توی این حالت origin ـه) روش گوش می‌ده و منتظر کلادفلیر می‌مونه.

- **ورودی:** `string` به شکل `ip:port` — مثلاً `0.0.0.0:2087`
- **پیش‌فرض:** `0.0.0.0:8443`
- پورتش **باید با `cdn.port` سمت سرور یکی باشه**
- این پورت باید توی فایروال باز باشه و DNS دامنه باید به **همین ماشین** اشاره کنه (با ابر نارنجی روشن)

**چرا فیلد جداست و از `addr` استفاده نمی‌شه؟**
چون `paths[0].addr` توی همه‌ی حالت‌ها باید **آدرس واقعی سرور** رو نگه داره — هم چک لایسنس و هم شناسایی peer از اونجا می‌خونن. یه بار جاش آدرس bind گذاشته شد و نتیجه‌ش خطای `invalid server signature` بود. برای همین آدرس گوش‌دادن origin فیلد خودش رو داره.

---

## 6️⃣ `cdn.host` — 🔴 فقط سرور

دامنه‌ای که پشت کلادفلیره و **همون چیزیه که مسیر رو تعیین می‌کنه**.

- **ورودی:** `string` — مثلاً `cdn.example.com`
- ⚠️ رکورد DNS این دامنه باید به **سرور خارج (کلاینت)** اشاره کنه، نه به این ماشین — با **ابر نارنجی روشن**

**نکته‌ی کلیدی:** کلادفلیر anycast ـه. هر edge IP، هر دامنه‌ی پشت کلادفلیر رو قبول می‌کنه و از روی **نام دامنه** تصمیم می‌گیره درخواست کجا بره — نه از روی IP ای که بهش وصل شدی. پس:

> `edge_ips` فقط **در** هستن، `host` تعیین می‌کنه **کجا** می‌ری.

---

## 7️⃣ `cdn.port` — 🔴 فقط سرور

پورتی که سرور از طریق کلادفلیر بهش وصل می‌شه. باید **دقیقاً همون پورتی باشه که کلاینت توی `origin_bind` گذاشته**.

- **ورودی:** `int`
- **پیش‌فرض:** `443`

⚠️ کلادفلیر فقط این پورت‌ها رو فوروارد می‌کنه:

| | پورت‌ها |
|---|---|
| **HTTPS** (`xhttps`) | `443` · `2053` · `2083` · `2087` · `2096` · `8443` |
| **HTTP** (`xhttp`) | `80` · `8080` · `8880` · `2052` · `2082` · `2086` · `2095` |

هر پورت دیگه‌ای بذاری، اتصال به edge می‌رسه و **همون‌جا drop می‌شه** — و دقیقاً شبیه فیلترینگ به‌نظر میاد. برنامه خودش این رو چک می‌کنه و بهت هشدار می‌ده.

---

## 8️⃣ `cdn.edge_ips` — 🔴 فقط سرور

لیست آدرس‌های edge کلادفلیر که سرور برای بیرون رفتن استفاده می‌کنه.

- **ورودی:** `[]string` — مثلاً `["104.17.12.5", "162.159.36.7", "172.67.180.44"]`
- به **ترتیبی که نوشتی** امتحان می‌شن؛ اگه یکی از کار افتاد، بعدی استفاده می‌شه (بلک‌لیست موقت می‌خوره و بعداً دوباره تست می‌شه)

**هیچ اسکن یا اندازه‌گیری خودکاری انجام نمی‌شه.** اینکه کدوم آدرس‌ها از شبکه‌ی تو قابل دسترسن، چیزیه که فقط خودت می‌دونی — پس همین‌جا می‌نویسیش.

💡 حداقل ۲-۳ تا بذار. اگه فقط یکی بذاری و اون بلاک شه، تانل هیچ راه دیگه‌ای نداره.

---

## ⚙️ ایندکس‌های اختیاری (معمولاً دست نمی‌زنی)

اینا رو ننویسی، مقدار پیش‌فرض اعمال می‌شه:

| ایندکس | ورودی | پیش‌فرض | کارش |
|---|---|---|---|
| `host` | `string` | — | هدر Host و SNI رو عوض می‌کنه (حالت مستقیم). توی حالت CDN، `cdn.host` اولویت داره |
| `user_agent` | `string` | مرورگر معمولی | هویت مرورگری که توی هر درخواست فرستاده می‌شه |
| `buffer_bytes` | `int` | `32768` (۳۲KB) | چقدر دیتا توی هر جهت صف بشه. **کف تأخیر رو همین تعیین می‌کنه** — کمترش کنی پینگ بهتر می‌شه، به قیمت کمی throughput |
| `up_max_bytes` | `int` | `65536` (۶۴KB) | سقف حجم هر تیکه توی آپلود پکتی |
| `up_concurrency` | `int` | CDN: `8` / مستقیم: `2` | چند آپلود هم‌زمان در پرواز باشه. روی مسیر پرتأخیر بیشترش throughput رو بالا می‌بره |
| `separate_conns` | `bool` | `true` | آپلود و دانلود روی دو کانکشن جدا، تا آپلود سنگین جلوی دانلود صف نبنده |
| `pad_min` / `pad_max` | `int` | `8` / `48` | طول query string رو تصادفی می‌کنه تا همه‌ی درخواست‌ها یه اندازه نباشن |
| `probe_ms` | `int` | `8000` | چقدر منتظر تأیید سرور بمونه قبل از سوییچ به شکل دیگه‌ی آپلود |
| `socket_buf_bytes` | `int` | `131072` (۱۲۸KB) | بافر ارسال/دریافت کرنل روی کانکشن‌های این ترنسپورت |

---

## 📋 نمونه‌ی کامل — حالت کلادفلیر

### سرور (ایران) — دیال می‌کنه

```json
"xhttp": {
  "path": "/api/v2",
  "mode": "auto",
  "allow_insecure_tls": false,
  "cdn": {
    "enabled": true,
    "host": "cdn.example.com",
    "port": 2087,
    "edge_ips": ["104.17.12.5", "162.159.36.7", "172.67.180.44"]
  }
}
```

### کلاینت (خارج) — گوش می‌ده، origin ـه

```json
"xhttp": {
  "path": "/api/v2",
  "mode": "auto",
  "cdn": {
    "enabled": true,
    "origin_bind": "0.0.0.0:2087"
  }
}
```

**دقت کن:** کلاینت نه `host` داره، نه `port`، نه `edge_ips` — چون هیچ‌وقت دیال نمی‌کنه. اضافه کردنشون فقط باعث اشتباه می‌شه.

---

## ✅ چک‌لیست راه‌اندازی کلادفلیر

- [ ] `path` روی هر دو سمت **دقیقاً یکی**
- [ ] `cdn.enabled: true` روی **هر دو سمت**
- [ ] `cdn.port` سرور == پورتِ `origin_bind` کلاینت
- [ ] پورت جزو لیست پورت‌های مجاز کلادفلیر باشه
- [ ] DNS دامنه به **سرور خارج (کلاینت)** اشاره کنه، ابر نارنجی **روشن**
- [ ] پورت origin توی فایروال کلاینت **باز** باشه
- [ ] گواهی TLS روی **کلاینت** نصب باشه (origin ـه). self-signed کافیه اگه SSL mode کلادفلیر روی **Full** باشه
- [ ] حداقل ۲-۳ تا edge IP قابل دسترس

### اگه `peer_ips` استفاده می‌کنی

- [ ] `peer_ips` سرور = IP کلاینت
- [ ] `public_ip` کلاینت = **همون IP خودش** (نه IP سرور!)

این دو تا باید مو به مو یکی باشن، وگرنه این خطا رو می‌گیری:

```
Refused a peer: client says it is "", which is not in the allowed list [...]
```

که یعنی `public_ip` روی کلاینت ست نشده.
